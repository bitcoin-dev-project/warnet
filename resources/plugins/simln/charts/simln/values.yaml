name: "simln"
image:
  repository: "bitcoindevproject/simln"
  tag: "0.2.3"
  pullPolicy: IfNotPresent

restartPolicy: Never

workingVolume:
  name: working-volume
  mountPath: /working
configmapVolume:
  name: configmap-volume
  mountPath: /configmap

