name: "simln"
image:
  repository: "bitcoindevproject/simln"
  tag: "0.2.4"
  pullPolicy: IfNotPresent

workingVolume:
  name: working-volume
  mountPath: /working
configmapVolume:
  name: configmap-volume
  mountPath: /configmap

