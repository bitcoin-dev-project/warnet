apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bitcoincore.fullname" . }}-test-connection"
  labels:
    {{- include "bitcoincore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bitcoincore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
